/*
 * Exercise #1
 * Write the function translateNumber(number) that receives in input a number between 0 and 5 and
 * returns the corresponding string or 'invalid' for other numbers
 * 0 -> "zero"
 * 4 -> "four"
 * 7 -> "invalid"
 */
export function translateNumber(number) {
    // YOUR CODE HERE
    return;
}

/*
 * Exercise #2
 * Write the function isVowel(char) that returns true if the input character is a vowel and false otherwise
 * "a" -> true
 * "b" -> false
 */
export function isVowel(char) {
    // YOUR CODE HERE
    return;
}

/*
 * Exercise #3
 * Write the function printArray(array) that returns a string
 * made of every element of the input array separated by a space
 * [1,2,3,4,"ciao"] -> "1 2 3 4 ciao"
 */
export function printArray(array) {
    // YOUR CODE HERE
    return;
}

/*
 * Exercise #4
 * Write the function printReverse(array) that returns a string
 * made of every element of the input array separated by a space
 * starting from the last one
 * [1,2,3,4,"ciao"] -> "ciao 4 3 2 1"
 */
export function printReverse(array) {
    // YOUR CODE HERE
    return;
}

/*
 * Exercise #5
 * Write an object of type Person that has two properties: name, surname
 * The Person constructor accepts two arguments: name, surname
 * Define a toString() method on Person that returns the concatenation of name and surname
 */

export function Person(name, surname) {
    //YOUR CODE HERE
    return;
}

/*
 * Exercise 6
 * Write the function getTop(array) that returns the max value in the array
 * Assume the array contains only numbers
 * [2,5,1,5,7] -> 7
 */
export function getTop(array) {
    // YOUR CODE HERE
}

/*
 * Exercise 7
 * Write the function getSecondTop(array) that returns the second highes value in the array
 * Assume the array contains only numbers
 * [2,3,1,5,7] -> 5
 * [2,7,1,5,7] -> 5
 */
export function getSecondTop(array) {
    // YOUR CODE HERE
}

/*
 * Exercise 8
 * Write the function getPow(array) that raises each value of the input array
 * to the power of its index and returns the resulting array
 * Assume the array contains only numbers
 * [1,2,3,4,5] -> [1, 2, 9, 64, 625]
 */
export function getPow(array) {
    // YOUR CODE HERE
}

/*
 * Exercise 9
 * Write the function getPortion(array, startIndex, endIndex) that returns a subarray
 * starting from startIndex to endIndex (both included)
 * Assume the array contains only numbers
 * getPortion([1,2,3,4,5,6,7], 3, 5) -> [4,5,6]
 */
export function getPortion(array, startIndex, endIndex) {
    // YOUR CODE HERE
}

/*
 * Exercise 10
 * Write the function getSum(array) that returns the sum of every element in the array
 * Assume the array contains only numbers
 * getSum([1,2,3,4,5,6,7]) -> 28
 */
export function getSum(array) {
    // YOUR CODE HERE
}

/*
 * Exercise 11
 * Write the function getEven(array) that returns and array containg only the even numbers of the input array
 * Assume the array contains only numbers
 * getEven([1,2,3,4,5,6,7]) -> [2,4,6]
 */
export function getEven(array) {
    // YOUR CODE HERE
}

/*
 * Exercise 12
 * Write the function getDone(array) that takes an array of objects with the following shape
 * {
 *    task: 'Task name',
 *    done: true|false
 * }
 * and returns only the completed ones
 */
export function getDone(array) {
    // YOUR CODE HERE
}

/*
 * BONUS Exercise
 * Write the function howManyCanIBuy(budget, wishlist) where budget is an integer
 * and wishlist is an array of objects with the following shape:
 * {
 *    description: 'Item Description',
 *    price: 125
 * }
 * The function returns the maximum number of items I can buy with a given budget
 * (trying to spend as little as possible) and the total price
 * {
 *   numberOfItems: 3
 *   totalPrice: 125
 * }
 */
export function howManyCanIBuy(array) {
    // YOUR CODE HERE
}
